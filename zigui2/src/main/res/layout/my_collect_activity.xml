<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:background="@color/backgroud_white"
    android:layout_height="match_parent"
    android:orientation="vertical" >
	<!-- 我的收藏 -->
    <include
        layout="@layout/title" />

    <LinearLayout 
        android:id="@+id/mycollect_ll_null"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:visibility="gone"
        >
        <ImageView 
            android:layout_width="wrap_content"
        	android:layout_height="wrap_content"
        	android:background="@drawable/mycollect_null_bg"
            />
        <TextView 
            android:layout_width="wrap_content"
        	android:layout_height="wrap_content"
        	android:text="暂无收藏的资源"
        	android:textColor="@color/font_darkgray"
            android:textSize="@dimen/three_text_type"
            />
    </LinearLayout>
    <com.wcyc.zigui2.widget.FallListView
        android:id="@+id/lv_mycollects"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:listSelector="@drawable/falling_list_selector"
        android:divider="@color/font_lightgray"
        android:dividerHeight="0.5dp" 
        android:scrollbars="none"
        />
</LinearLayout>